<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= title || 'Enterprise File Processor' %></title>
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <header>
            <nav class="navbar">
                <div class="nav-brand">
                    <h1> Enterprise System</h1>
                </div>
                <div class="nav-links">
                    <% if (locals.user) { %>
                        <a href="/">Dashboard</a>
                        <a href="/upload">Upload</a>
                        <a href="/api/system">API</a>
                        <a href="/logout">Logout</a>
                    <% } else { %>
                        <a href="/login">Login</a>
                        <a href="/register">Register</a>
                    <% } %>
                </div>
            </nav>
        </header>

        <main class="container">
            <%- body %>
        </main>

        <footer>
            <p>&copy; 2025 Enterprise File Processor - Built with JavaScript Pillars</p>
        </footer>

        <% if (locals.user) { %>
            <script src="/js/dashboard.js"></script>
        <% } %>
    </body>
</html>

